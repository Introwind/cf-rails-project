<div class="container">
	<div class="row">
		<div class="thumbnail">
			<h1>Our best value bike!</h1>
			<h2><%= @product_sale.name %></h2>
			<p><%= @product_sale.description %></p>
			<%= image_tag(@product_sale.image_url, class: "img-responsive")	%>
			<p>Only £<%= sprintf('%.2f', (@product_sale.price / 100) ) %></p>
		</div>
	</div>
</div>

