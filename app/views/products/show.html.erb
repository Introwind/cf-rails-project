<div class="container">

    <div class="row">
        <div class="col-md-2">
            <p class="lead">Navigation</p>
            <div class="list-group">
                <%= link_to 'Back', products_path, class: "list-group-item" %>
                <% if admin? %>
                  <%= link_to 'Edit', edit_product_path(@product), class: "list-group-item" %>
                  <%= link_to 'Delete', products_path, method: :delete, data: { confirm: "Are you sure?" }, class: "list-group-item" %>    
                <% end %>
            </div>
        </div>

        <div class="col-md-10">
            <div class="thumbnail mainprod">
                <%= image_tag(@product.image_url, class: "img-responsive") %>
                <div class="caption-full">
                    <h4 class="pull-right">£ <%= @product.price %></h4>
                    <h4><%= @product.name %></a>
                    </h4>
                    <p><%= @product.description %></p>
                </div>
                <div class="ratings">
                    <p class="pull-right"><%= @product.comments.count %> reviews</p>
                    <p>
                    <div class="rated" data-score="<%= @product.average_rating %>"></div>
                    <%= @product.average_rating.to_i %>.0 stars
                    </p>
                </div>
            </div>

            <div class="well">
                <div class="text-right">
                <%= render 'new_comment' %>
                </div>

                    <h4>Product Reviews:</h4>
                <hr>

                <div class="row">
                    <div class="col-md-12">
                      <%= render 'comments' %>
                    </div>
                </div>
            </div>
        </div>

    </div>

</div>

      
